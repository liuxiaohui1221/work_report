# 项目进度管理系统 - 改造方案

## 背景与目标

**现状：** 现有系统主要是读取Git提交和Excel规划文件来生成静态报告，功能相对简单。

**目标：** 升级为实时项目进度管理平台，支持：
1. AI对话式添加/编辑任务
2. 多智能体协作（项目经理、架构师、进度追踪、测试专家）
3. 任务依赖关系与阻塞检测
4. 开发人员实时查看自己和团队的任务进度
5. 消息推送（bug通知、新任务、催促）

---

## 一、系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                      前端 (Web界面)                          │
│  - 开发者仪表盘（我的任务、团队进度、待办事项）                │
│  - AI对话窗口（添加任务、查询进度、催促）                      │
│  - 消息中心（bug通知、新任务、系统通知）                       │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Flask 后端 API                            │
│  - 任务管理 (CRUD, 依赖关系)                                  │
│  - 智能体调度 (Agent Coordination)                           │
│  - Excel 导入/导出/编辑                                      │
│  - 消息通知 (Messages)                                       │
│  - LLM 对话 (Chat Interface)                                │
└─────────────────────────────────────────────────────────────┘
                              │
         ┌────────────────────┼────────────────────┐
         ▼                    ▼                    ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│  Task Database  │  │   Agent Brain   │  │   Git/Code      │
│  (SQLite/JSON)  │  │   (LLM Calls)   │  │   Integration   │
└─────────────────┘  └─────────────────┘  └─────────────────┘
```

---

## 二、智能体设计

### 2.1 智能体角色

| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| **项目经理 (PM)** | 协调其他专家智能体，汇总进度，决策 | 各智能体报告、项目数据 | 项目整体报告、决策建议 |
| **系统架构师** | 设计/优化系统架构、技术选型 | 需求、代码结构 | 架构建议、技术方案 |
| **进度追踪员** | 跟踪任务进度、识别风险 | Git提交、任务状态 | 进度报告、风险预警 |
| **测试专家** | 发现bug、验证修复 | 代码变更、测试结果 | Bug报告、修复建议 |

### 2.2 智能体工作流

```
用户提问/任务
     │
     ▼
┌─────────────┐
│  调度中心    │ ← 理解意图，分发任务
└─────────────┘
     │
     ├──→ 项目经理智能体（协调）
     │         │
     │         ▼
     │    ┌─────────────────┐
     │    │  需要架构师？    │ ──→ 架构师智能体
     │    │  需要进度追踪？  │ ──→ 进度追踪智能体
     │    │  需要测试专家？  │ ──→ 测试专家智能体
     │    └─────────────────┘
     │
     └──→ 直接执行（简单查询/任务添加）
```

---

## 三、数据模型

### 3.1 核心表结构

```sql
-- 项目表
projects (
    id, name, description, repo_path, created_at, updated_at
)

-- 任务表
tasks (
    id, project_id, title, description,
    assignee_id, status, priority,
    depends_on, blocked_by,
    created_at, updated_at, due_date
)

-- 任务历史（变更记录）
task_history (
    id, task_id, field_changed, old_value, new_value,
    changed_by, changed_at, reason
)

-- 消息/通知
messages (
    id, type, title, content, sender_id, receiver_id,
    related_task_id, is_read, created_at
)

-- 人员表
developers (
    id, name, email, git_name, role, project_id,
    created_at, last_active
)

-- Bug记录
bugs (
    id, task_id, title, description, severity,
    reporter, assignee, status, fix_suggestion,
    created_at, resolved_at
)
```

### 3.2 任务状态流转

```
待认领 → 开发中 → 待联调 → 测试中 → 已完成
            ↑                    ↓
            └───── 被阻塞 ───────┘
```

---

## 四、功能模块

### 4.1 AI对话添加任务

**流程：**
1. 用户在对话窗口输入：`"帮我在platform项目添加一个任务：完成用户登录功能，负责人是刘小辉，截止日期下周五"`
2. LLM解析意图 → 提取任务信息
3. 写入tasks表
4. 返回任务创建结果

**Prompt示例：**
```
你是任务解析助手。用户输入可能包含：
- 任务标题
- 负责人
- 截止日期
- 项目名称
- 优先级

请从用户输入中提取这些信息，返回JSON格式。
```

### 4.2 Excel导入与编辑

**流程：**
1. 上传Excel → 解析任务列表
2. 在前端展示可编辑的表格
3. 用户修改 → 保存
4. 记录变更历史

**Excel格式：**
| 模块 | 功能描述 | 状态 | 负责人 | 截止日期 | 依赖 |
|------|---------|------|--------|---------|------|
| 登录 | 用户名密码登录 | 开发中 | 刘小辉 | 2026-05-20 | 用户模块 |

### 4.3 任务依赖与阻塞

**依赖建模：**
- 任务A 依赖 任务B（B完成前A不能开始）
- 阻塞检测：实时检查前置任务状态

**催促功能：**
- 用户点击"催促" → 发送消息给阻塞者
- 消息内容：`"【催促】任务 [XXX] 阻塞了你的任务 [YYY]，请尽快完成！"`

### 4.4 消息中心

**消息类型：**
1. **系统通知** - 任务变更、状态更新
2. **Bug通知** - 测试专家发现的bug
3. **催促消息** - 来自其他开发者的催促
4. **AI助手** - LLM对任务的分析和建议

**展示方式：**
- 右上角铃铛图标，显示未读数
- 点击展开消息列表
- 支持已读/未读筛选

---

## 五、前端页面结构

### 5.1 页面组成

```
┌──────────────────────────────────────────────────────────────┐
│  Header: Logo | 项目选择 | 搜索 | 🔔消息 | 👤用户           │
├──────────────────────────────────────────────────────────────┤
│ ┌──────────┐ ┌────────────────────────────────────────────┐ │
│ │ 侧边栏    │ │  主内容区                                  │ │
│ │          │ │                                            │ │
│ │ 📊 仪表盘  │ │  [仪表盘] - 我的任务进度、团队进度           │ │
│ │ 📋 任务    │ │  [任务列表] - 个人任务、团队任务            │ │
│ │ 💬 AI对话  │ │  [AI对话] - 聊天式添加任务、查询进度        │ │
│ │ 📁 文件    │ │  [文件管理] - Excel上传/编辑               │ │
│ │ 📈 报告    │ │  [报告中心] - 自动生成的项目报告            │ │
│ │ ⚙️ 设置    │ │                                            │ │
│ └──────────┘ └────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

### 5.2 开发者仪表盘

**核心指标：**
- 我的任务总数、进行中、已完成、阻塞
- 本周完成率
- 阻塞我的任务（前置任务未完成）
- 我阻塞的任务（其他人等待）

**视图：**
- 甘特图（可选）
- 列表视图
- 看板视图（状态分组）

---

## 六、技术实现路径

### 阶段一：基础框架（1-2天）
1. 新增数据库表（tasks, task_history, messages, bugs）
2. 重构前端架构（路由、组件化）
3. 任务CRUD API

### 阶段二：AI对话集成（2-3天）
1. LLM对话接口
2. 任务解析与创建
3. 进度查询与回复

### 阶段三：智能体系统（3-5天）
1. 智能体调度框架
2. 进度追踪智能体（定时扫描Git）
3. 测试专家智能体（代码分析、bug检测）
4. 项目经理智能体（汇总报告）

### 阶段四：高级功能（2-3天）
1. 任务依赖图
2. 消息推送系统
3. Excel导入/编辑
4. 催促功能

---

## 七、关键文件变更

### Backend
- `main/app.py` - 新增API端点
- `main/models.py` - 数据模型（新增）
- `main/agents/` - 智能体实现（新增目录）
- `main/tasks.py` - 任务管理逻辑

### Frontend
- `web/index.html` - 完全重构
- `web/js/` - 组件JS（新增）
- `web/css/` - 样式文件

### Database
- `work_report.db` - 新增表

---

## 八、验证方案

1. **功能测试：**
   - 上传Excel，编辑，保存，验证历史记录
   - AI对话添加任务，验证创建正确
   - 任务依赖设置，验证阻塞逻辑

2. **智能体测试：**
   - 进度追踪：修改代码，验证自动发现
   - Bug检测：提交buggy代码，验证报告

3. **前端测试：**
   - 开发者登录，查看自己的任务
   - 消息通知，点击跳转

---

## Next Step

方案确认后，我将开始：
1. 创建数据库模型
2. 实现后端API
3. 重构前端界面
4. 集成AI对话
5. 开发智能体系统

是否先讨论某些具体模块的实现细节？